client: metlife
# The as-of date comes from the row-1 banner text ("Report Date:
# 02/01/2026"), not a column -- this format applies to that extracted
# text, same as it would to any date-typed column this client provided.
dateFormat: MM/dd/yyyy

feeds:
  holdings:
    modelId: Holdings
    worksheetNames:
      - Holdings
